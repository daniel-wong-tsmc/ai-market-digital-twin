"""F110 chart-data support: the curated registry of series that may be
drawn as a small chart next to a dashboard bullet.

This package only declares what exists (see ``registry.py``). Fetching the
actual data points is a later task.
"""
